'use strict';

module.exports = {
  // Use a unique extension so we don't affect other files when fingerprinting.
  MANIFEST_TEMP_NAME: 'manifest.ember-web-app.json',
  MANIFEST_NAME: 'manifest.json'
};
